export type registerType = {
    username: string;
    password: string;
    konfirmasiPassword: string;
};

export type uploadPhotoType = {
    id: number;
    photo: File;
};

export type loginType = {
    username: string;
    password: string;
};
